<% provide(:title, 'All users') %>
<h1>All users</h1>

<!--il looks for an user object
and displays pagination links to access
other pages-->
<%= will_paginate %>

<ul class="users">
    <!--render the user partial for each user-->
    <%= render @users %>
</ul>

<%= will_paginate%>